name: Read Stats Text Updater
on:
  schedule:
    - cron: "15 */12 * * *"
  workflow_dispatch:
jobs:
  acitivity_update:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
        with:
          fetch-depth: 0
      - uses: jamesgeorge007/github-activity-readme@master
        env:
          GITHUB_TOKEN: ${{ secrets.MAT_TOKEN  }}
        with:
          TARGET_FILE: ./TEMPLATE.md

  text_stats:
    needs: acitivity_update
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
        with:
          fetch-depth: 0
      - name: Generate README.md
        uses: teoxoy/profile-readme-stats@master
        with:
          token: ${{ secrets.MAT_TOKEN }}
          includeForks: true
      - name: Update README.md
        run: |
          if [[ "$(git status --porcelain)" != "" ]]; then
          git config user.name github-actions[bot]
          git config user.email 41898282+github-actions[bot]@users.noreply.github.com
          git add .
          git commit -m "Update README activity"
          git push
          fi
